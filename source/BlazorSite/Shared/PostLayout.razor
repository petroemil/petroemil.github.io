@inherits BlazorSite.Shared.PostLayoutBase

    <div class="content-card">

        @if (Metadata is null)
        {
            <p>Loading post content...</p>
        }
        else
        {
            <div class="content-heroimage" style="background-image:url('@($"content/{Metadata.PostId}/{Metadata.HeroImageUrl}")')" />

            <div class="content-title">
                <p>
                    <b>@Metadata.Title</b>
                    <br />
                    @Metadata.PublishDate.ToLocalTime().ToString("f")
                </p>
            </div>

            @ChildContent
        }
    </div>