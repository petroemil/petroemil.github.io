name: ASP.NET Core CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.0.100-preview8-013656
    - name: Setup ASP.NET Core
      run: run: dotnet new -i Microsoft.AspNetCore.Blazor.Templates::3.0.100-preview8-013656
    - name: Build
      run: dotnet build --configuration Release --force source/BlazorSite.sln
